# Carlos Brizuela - Antimicrobial Peptide Optimization Package
# Requirements for self-contained operation
#
# Core dependencies (required for all features):
numpy>=1.20.0
scipy>=1.7.0

# ML dependencies (required for prediction):
scikit-learn>=1.0.0
joblib>=1.0.0

# NSGA-II multi-objective optimization:
deap>=1.4.0

# Optional: PyTorch (for PeptideVAE encoder)
# Install separately for GPU support:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
# Or for CPU only:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# Optional: For data downloading
# requests>=2.27.0

# Optional: For visualization
# matplotlib>=3.5.0
# seaborn>=0.12.0
