# Alejandra Rojas - Arbovirus Surveillance Package
# Requirements for self-contained operation
#
# Core dependencies (required for all features):
numpy>=1.20.0

# NCBI and sequence analysis:
biopython>=1.80

# Optional: For data analysis and visualization
# pandas>=1.4.0
# matplotlib>=3.5.0
# seaborn>=0.12.0

# Optional: For web requests
requests>=2.27.0

# Optional: PyTorch (for TrainableCodonEncoder research integration)
# Install separately for GPU support:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
# Or for CPU only:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
